<?xml version="1.0" encoding="utf-8"?>
<!--
  Conduit for Android.

  Each permission arrives with the feature that needs it, together with an
  explanation shown before any prompt — Conduit asks only when a feature is
  used.

  WRITE_SECURE_SETTINGS lets the Wireless debugging and stay-awake toggles
  work. Android never grants it to an app on its own: it is granted over adb,
  from Conduit for Mac, when the user asks. Until then the toggles explain
  that instead of failing silently.

  ACCESS_NETWORK_STATE reads the Wi-Fi link's signal strength and speed for
  the home screen. It is granted at install and reveals no network name.

  The touch guard is an accessibility service that ignores touches on the
  phone's own screen while Conduit for Mac mirrors it with the screen off.
  Only the Mac turns it on, for as long as the screen is off; it reads
  nothing on the screen. Its lease receiver accepts only senders that hold
  WRITE_SECURE_SETTINGS, which in practice is the adb shell.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conduit">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".guard.TouchGuardService"
            android:exported="true"
            android:label="@string/touch_guard_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/touch_guard_service" />
        </service>

        <receiver
            android:name=".guard.TouchGuardLease"
            android:exported="true"
            android:permission="android.permission.WRITE_SECURE_SETTINGS"
            tools:ignore="ExportedReceiver" />
    </application>
</manifest>
