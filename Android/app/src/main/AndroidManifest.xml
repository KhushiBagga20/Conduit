<?xml version="1.0" encoding="utf-8"?>
<!--
  Conduit for Android.

  Each permission arrives with the feature that needs it, together with an
  explanation shown before any prompt — Conduit asks only when a feature is
  used.

  WRITE_SECURE_SETTINGS lets the Wireless debugging and stay-awake toggles
  work. Android never grants it to an app on its own: it is granted over adb,
  from Conduit for Mac, when the user asks. Until then the toggles explain
  that instead of failing silently.

  ACCESS_NETWORK_STATE reads the Wi-Fi link's signal strength and speed for
  the home screen. It is granted at install and reveals no network name.

  The touch guard is an accessibility service that ignores touches on the
  phone's own screen while Conduit for Mac mirrors it with the screen off.
  Only the Mac turns it on, for as long as the screen is off; it reads
  nothing on the screen. Its lease receiver accepts only senders that hold
  WRITE_SECURE_SETTINGS, which in practice is the adb shell.

  Conduit Link — the channel to Conduit for Mac — needs INTERNET to dial the
  Mac, and runs as a connected-device foreground service so the link does not
  depend on a visible screen; that service type requires CHANGE_NETWORK_STATE,
  which is granted at install. POST_NOTIFICATIONS is asked for when a Mac is
  added, so the "Linked to" notification can show. MacHint, like the lease,
  accepts only senders holding WRITE_SECURE_SETTINGS.

  "Send to Mac" in the share sheet sends the web link in shared text to the
  linked Mac, which opens it; links from the Mac arrive as a notification
  that opens them here. Neither keeps the link, and only its site is shown.

  BLUETOOTH_ADVERTISE and BLUETOOTH_CONNECT ("Nearby devices") let a paired
  Mac ask for this phone's hotspot when the Mac is offline. The advert
  carries only Conduit's service UUID, and only a request signed by a paired
  Mac and addressed to this phone shows a notification.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- The hotspot switch, opened straight from the request notification. -->
    <queries>
        <intent>
            <action android:name="com.android.settings.TETHER_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.WIRELESS_SETTINGS" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conduit">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".link.ShareToMacActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/share_to_mac"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".guard.TouchGuardService"
            android:exported="true"
            android:label="@string/touch_guard_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/touch_guard_service" />
        </service>

        <service
            android:name=".link.LinkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <receiver
            android:name=".link.MacHint"
            android:exported="true"
            android:permission="android.permission.WRITE_SECURE_SETTINGS"
            tools:ignore="ExportedReceiver" />

        <receiver
            android:name=".guard.TouchGuardLease"
            android:exported="true"
            android:permission="android.permission.WRITE_SECURE_SETTINGS"
            tools:ignore="ExportedReceiver" />
    </application>
</manifest>
